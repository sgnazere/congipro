# URL de l'API. Par défaut « /api » : le serveur de dev Vite (et le reverse proxy
# en production) redirige /api vers le backend Node.
VITE_API_URL=/api
VITE_ORG_NAME=EcoGec SARL
# Ville imprimée sur les PDF de demande (« Fait à … »)
VITE_ORG_CITY=Abidjan
